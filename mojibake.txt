1: const searchInput = document.getElementById('searchInput');\n2: const resultsContainer = document.getElementById('results');\n3: const statsContainer = document.getElementById('stats');\n4: const modal = document.getElementById('modal');\n5: const modalBody = document.getElementById('modalBody');\n6: const modalTitle = document.getElementById('modalTitle');\n7: const modalOpenOriginal = document.getElementById('modalOpenOriginal');\n8: const modalClose = document.getElementById('modalClose');\n80: document.getElementById('tabProduct').classList.toggle('active', tabName === 'product');\n81: document.getElementById('tabRequest').classList.toggle('active', tabName === 'request');\n82: document.getElementById('tabGlobal').classList.toggle('active', tabName === 'global');\n83: document.getElementById('globalFilters').style.display = tabName === 'global' ? 'flex' : 'none';\n86: q: document.getElementById('searchInput').value.trim(),\n87: seiban: document.getElementById('searchSeiban').value.trim(),\n88: req_no: document.getElementById('searchReq').value.trim(),\n89: product: document.getElementById('searchProduct').value.trim(),\n91: company: document.getElementById('searchCompany').value.trim()\n97: inputs.forEach(id => {\n98: const el = document.getElementById(id);\n100: el.addEventListener('input', () => {\n124: tokens.forEach(t => {\n127: const regex = new RegExp(t.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'gi');\n136: const list = document.getElementById('partInputsList');\n143: const row = document.createElement('div');\n147: const input = document.createElement('input');\n153: input.addEventListener('input', () => {\n158: const removeBtn = document.createElement('button');\n165: document.getElementById('addPartBtn').disabled = false;\n173: document.getElementById('addPartBtn').disabled = true;\n179: ['searchInput', 'searchSeiban', 'searchReq', 'searchProduct', 'searchCompany'].forEach(id => {\n180: document.getElementById(id).value = '';\n182: const list = document.getElementById('partInputsList');\n189: document.querySelector('.searchPartInput').addEventListener('input', () => {\n193: document.getElementById('addPartBtn').disabled = false;\n212: const seiban = document.getElementById('searchSeiban').value.trim();\n213: const req = document.getElementById('searchReq').value.trim();\n214: const product = document.getElementById('searchProduct').value.trim();\n217: const company = document.getElementById('searchCompany').value.trim();\n218: const q = document.getElementById('searchInput').value.trim();\n221: document.getElementById('results').innerHTML = '<div class="empty-state">検索条件を入力してください</div>';\n229: return String.fromCharCode(s.charCodeAt(0) - 0xFEE0);\n352: document.getElementById('searchInput').value = '';\n353: document.getElementById('searchSeiban').value = '';\n354: document.getElementById('searchReq').value = '';\n355: document.getElementById('searchProduct').value = '';\n356: document.getElementById('searchCompany').value = '';\n358: document.getElementById('searchInput').value = '';\n359: document.getElementById('searchSeiban').value = '';\n360: document.getElementById('searchReq').value = '';\n361: document.getElementById('searchProduct').value = '';\n362: document.getElementById('searchCompany').value = '';\n364: const list = document.getElementById('partInputsList');\n371: document.querySelector('.searchPartInput').addEventListener('input', () => {\n375: document.getElementById('addPartBtn').disabled = false;\n379: document.getElementById('searchReq').value = keyword.substring(1);\n381: document.getElementById('searchInput').value = keyword;\n410: groups.forEach(group => {\n444: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n450: group.boms.forEach(b => {\n455: b.components.forEach(c => {\n482: ${b.sp_url ? `<a href="${b.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (b.file ? `<a href="/${b.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excelを開く</a>` : '')}\n506: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n512: normalReqs.forEach(r => {\n533: ${r.sp_url ? `<a href="${r.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (r.file ? `<a href="/${r.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excel/画像を開く</a>` : '')}\n544: <div class="section-header" onclick="this.parentElement.classList.toggle('open')" style="background-color: #fff3cd; color: #856404; border-left-color: #ffeeba;">\n550: handReqs.forEach(r => {\n565: b.components.forEach(c => {\n584: ${r.sp_url ? `<a href="${r.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (r.file ? `<a href="/${r.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excel/画像を開く</a>` : '')}\n598: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n604: group.inspections.forEach(i => {\n610: ${i.sp_url ? `<a href="${i.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (i.file_path ? `<a href="/${i.file_path.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">開く</a>` : '')}\n622: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n628: group.photos.forEach(p => {\n646: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n652: group.drawings.forEach(d => {\n679: groups.forEach(g => {\n681: g.requests.forEach(req => {\n711: reqBoxes.forEach(box => {\n735: ${r.sp_url ? `<a href="${r.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (r.file ? `<a href="/${r.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excel/画像を開く</a>` : '')}\n745: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n750: box.boms.forEach(b => {\n755: b.components.forEach(c => {\n778: ${b.sp_url ? `<a href="${b.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (b.file ? `<a href="/${b.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excelを開く</a>` : '')}\n790: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n795: box.inspections.forEach(i => {\n801: ${i.sp_url ? `<a href="${i.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (i.file_path ? `<a href="/${i.file_path.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">開く</a>` : '')}\n813: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n818: box.photos.forEach(p => {\n836: <div class="section-header" onclick="this.parentElement.classList.toggle('open')">\n841: box.drawings.forEach(d => {\n873: groups.forEach(g => {\n905: const showBom = document.getElementById('filterBom').checked;\n906: const showReq = document.getElementById('filterReq').checked;\n907: const showInsp = document.getElementById('filterInsp').checked;\n908: const showPhoto = document.getElementById('filterPhoto').checked;\n909: const showDrawing = document.getElementById('filterDrawing').checked;\n930: allBoms.forEach(b => {\n934: b.components.forEach(c => {\n961: ${b.sp_url ? `<a href="${b.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (b.file ? `<a href="/${b.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excelを開く</a>` : '')}\n976: normalReqs.forEach(r => {\n996: ${r.sp_url ? `<a href="${r.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (r.file ? `<a href="/${r.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excel/画像を開く</a>` : '')}\n1006: handReqs.forEach(r => {\n1027: ${r.sp_url ? `<a href="${r.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (r.file ? `<a href="/${r.file.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">Excel/画像を開く</a>` : '')}\n1040: allInsps.forEach(i => {\n1046: ${i.sp_url ? `<a href="${i.sp_url}" target="_blank" class="btn btn-primary" style="background:#0078d4; border-color:#0078d4;">🌐 ブラウザで開く (Excel for Web)</a>` : (i.file_path ? `<a href="/${i.file_path.replace(/\\/g, '/').split('/').map(encodeURIComponent).join('/')}" target="_blank" class="btn btn-secondary">開く</a>` : '')}\n1057: allPhotos.forEach(p => {\n1077: allDrawings.forEach(d => {\n1121: modalClose.addEventListener('click', () => {\n1125: modal.addEventListener('click', (e) => {\n1131: document.addEventListener('keydown', (e) => {\n1132: if (e.key === 'Escape') modal.classList.remove('active');\n1143: document.getElementById('linkModalPartNo').innerText = partNo;\n1144: document.getElementById('linkModalMasterId').value = '';\n1145: document.getElementById('linkModalError').style.display = 'none';\n1146: document.getElementById('linkModal').style.display = 'flex';\n1150: document.getElementById('linkModal').style.display = 'none';\n1155: const masterId = document.getElementById('linkModalMasterId').value.trim();\n1157: document.getElementById('linkModalError').innerText = 'マスタIDを入力してください。';\n1158: document.getElementById('linkModalError').style.display = 'block';\n1174: document.getElementById('linkModalError').innerText = '保存に失敗しました。';\n1175: document.getElementById('linkModalError').style.display = 'block';\n1182: const panel = document.getElementById('searchPanel');\n1183: const btn = document.getElementById('toggleSearchBtn');\n